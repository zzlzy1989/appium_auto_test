WEB - APP

1、app - 界面小，内容少，逻辑也简单，功能也没有web网页齐全

2、封装页面 ==

3、测试用例 =
    a、开启会话
        web : webdriver.Chrome()
        app : 准备启动参数()，desired_caps // 然后，Remote与appium 连接。
            平台名称
            平台版本号
            自动化引擎
            设备名称
            包名
            入口页面名
            不重置
            键盘
    b、登录功能：每一个用例，都应该是未登录状态，无论何时执行。
    c、首页功能：每一个用例，只需要启动APP即可。
    d、前置、后置：noReset = False
    f、最后䘝用例：登录成功

    g、权限功能：每一个用例、在登录状态下才可以；
    h、前置后置：noReset = True 的情况下，判断用户是否登录成功！
    i、如果没有登录成功，则重新登录一次。

4、yaml - 读取为字典


